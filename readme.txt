1. pip install "fastapi[all]"
2. python -m venv env
3. pip install fastapi uvicorn
4. pip freeze > requirements.txt
5. pip install pymongo // automatically installs bson
6. pip install pydantic
7. pip install dotenv